//Users service used for users REST endpoint
angular.module('mean.mean-admin').factory("OffersAdmin", ['$http',
    function($http) {

    function _getOffers(){
      
    }

    return {
      name: 'offersAdmin',
      
    };
  }
]);
